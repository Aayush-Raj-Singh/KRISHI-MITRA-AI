# Mobile (Capacitor)

The mobile app is now built from the web app using Capacitor.

Use the frontend app in `apps/frontend/` for Android builds.

Basic flow:
- `cd apps/frontend`
- `npm install`
- `npm run build`
- `npx cap sync`
- `npx cap open android`
